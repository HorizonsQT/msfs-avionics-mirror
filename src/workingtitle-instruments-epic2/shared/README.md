# Working Title Epic 2 Shared

This is the source code for the Working Title Epic 2.0 avionics system shared library for MSFS. To build it, run `npm i && npm run build`.

After the build is finished, the final output will be located in the `dist` directory. The included `.tgz` file is used as a dependency for the Epic 2 instruments project.
